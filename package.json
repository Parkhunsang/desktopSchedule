{
  "name": "desktopschedule",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "electron-main.cjs",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "desktop": "electron .",
    "desktop:dist": "npm run build && electron ."
  },
  "devDependencies": {
    "electron": "^31.0.1",
    "vite": "^8.0.12"
  }
}
